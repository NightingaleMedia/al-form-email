{
  "name": "email_form",
  "version": "1.0.0",
  "description": "emails al from the form",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx env-cmd functions-framework --target main --port 1234 --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/functions-framework": "^3.1.2",
    "@sendgrid/mail": "^7.7.0"
  },
  "devDependencies": {
    "env-cmd": "^10.1.0"
  }
}
